// Leitura dos valores A e B (você pode inserir os valores desejados)
var A = 5;
var B = 5;

// Verifica se A e B são iguais
if (A === B) {
    // Se forem iguais, some os valores
    var C = A + B;
} else {
    // Se forem diferentes, multiplique A por B
    var C = A * B;
}

// Mostra o resultado na tela
console.log("O resultado é: " + C);